# Hex-Inception
Hex Inception (macOS) is a plug-in (AUv2/VST3) designed for hexaphonic guitar audio DAW/host workflows.  It solves a specific problem: a hardware device may expose all of its channels correctly to macOS, while the DAW does not provide a practical 8-channel input path to a multichannel input plug-in that needs them.
